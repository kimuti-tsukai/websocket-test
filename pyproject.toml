[project]
name = "websocket-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.0"
dependencies = [
    "pynput>=1.8.1",
    "websockets>=15.0.1",
]
